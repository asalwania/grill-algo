import type { ProblemMeta } from '../../../lib/types'

export const meta: ProblemMeta = {
  slug: 'valid-sudoku',
  number: 36,
  title: 'Valid Sudoku',
  difficulty: 'Medium',
  pattern: 'Hash Map (Grid)',
  blurb:
    'Given a 9x9 Sudoku board, determine if the filled cells so far satisfy the rules: no row, column or 3x3 box may contain the same digit twice. Empty cells are ignored.',
}
