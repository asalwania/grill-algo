import type { ProblemMeta } from '../../../lib/types'

export const meta: ProblemMeta = {
  slug: 'valid-palindrome',
  number: 125,
  title: 'Valid Palindrome',
  difficulty: 'Easy',
  pattern: 'Two Pointers',
  blurb:
    'Given a string, return true if it reads the same forward and backward once every non-alphanumeric character is removed and the letters are lowercased.',
}
