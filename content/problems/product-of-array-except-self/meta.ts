import type { ProblemMeta } from '../../../lib/types'

export const meta: ProblemMeta = {
  slug: 'product-of-array-except-self',
  number: 238,
  title: 'Product of Array Except Self',
  difficulty: 'Medium',
  pattern: 'Prefix / Suffix Products',
  blurb:
    'Given an integer array, return an array where each element is the product of every other element — computed in linear time and without using division.',
}
