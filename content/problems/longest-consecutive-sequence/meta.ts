import type { ProblemMeta } from '../../../lib/types'

export const meta: ProblemMeta = {
  slug: 'longest-consecutive-sequence',
  number: 128,
  title: 'Longest Consecutive Sequence',
  difficulty: 'Medium',
  pattern: 'Hash Set',
  blurb:
    'Given an unsorted array of integers, return the length of the longest run of consecutive integers it contains — in O(n) time.',
}
